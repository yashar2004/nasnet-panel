# NasNet Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================================================
# Backend Server Configuration (Go)
# ============================================================================

# Server port (default: 8080 for dev, 80 for production containers)
PORT=8080

# Environment mode (development, production)
GO_ENV=development

# Go runtime settings
GOMAXPROCS=2

# ============================================================================
# Frontend Configuration (Vite + TypeScript)
# ============================================================================

# Backend API base URL (used by frontend to communicate with Go backend)
# Leave empty to auto-detect or use default localhost:8080
# Examples:
#   VITE_API_BASE_URL=http://localhost:8080
#   VITE_API_BASE_URL=http://192.168.1.100:8080
#   VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=

# ============================================================================
# Docker Build Configuration
# ============================================================================

# Target platforms for multi-architecture builds
# Format: linux/amd64,linux/arm64,linux/arm/v7
PLATFORMS=linux/amd64,linux/arm64,linux/arm/v7

# ============================================================================
# GitHub Actions / CI/CD Secrets
# ============================================================================

# GitHub Container Registry (automatically available in GitHub Actions)
# Format: owner/repo (e.g., username/nasnet)
GITHUB_REPOSITORY=your-username/nasnet

# Container registry publishing is GHCR-only.
# GitHub Actions uses GITHUB_TOKEN automatically for ghcr.io publishing.

# GitHub token (automatically provided by GitHub Actions)
# GITHUB_TOKEN is automatically provided - no need to set manually

# ============================================================================
# MikroTik RouterOS Default Configuration
# ============================================================================

# Default RouterOS credentials (for testing/development)
# Note: These are common defaults, change them in production!
MIKROTIK_DEFAULT_USERNAME=admin
MIKROTIK_DEFAULT_PASSWORD=

# Common RouterOS ports
# REST API: 80 (HTTP), 443 (HTTPS)
# API: 8728 (plain), 8729 (SSL)
# Winbox: 8291
# SSH: 22
# FTP: 21

# ============================================================================
# Development Settings
# ============================================================================

# Enable verbose logging
DEBUG=false

# Hot reload for backend (Air)
# air.toml configuration takes precedence

# Frontend dev server port (Vite)
VITE_PORT=5173

# ============================================================================
# Production Deployment Settings
# ============================================================================

# Container memory limits (for RouterOS container deployment)
# These are optimized for ARM64 routers with limited resources
CONTAINER_MEMORY_LIMIT=32M
CONTAINER_CPU_LIMIT=0.5

# ============================================================================
# Network Scanner Configuration
# ============================================================================

# Scanner worker pool size
SCANNER_WORKERS=4

# Scanner timeout (milliseconds)
SCANNER_TIMEOUT=5000

# Maximum concurrent scans
MAX_CONCURRENT_SCANS=10

# ============================================================================
# Security & Authentication
# ============================================================================

# Enable HTTPS for RouterOS connections (recommended)
ROUTEROS_USE_HTTPS=true

# Skip TLS verification for self-signed certificates (development only)
ROUTEROS_SKIP_TLS_VERIFY=false

# Session timeout (minutes)
SESSION_TIMEOUT=60

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================

# Custom Nx Cloud configuration (if using Nx Cloud)
# NX_CLOUD_ACCESS_TOKEN=your-nx-cloud-token

# Custom npm registry (if using private registry)
# NPM_REGISTRY=https://registry.npmjs.org/

# ============================================================================
# Notes:
# ============================================================================
# 
# 1. NEVER commit .env file to version control
# 2. Add .env to .gitignore
# 3. Use environment-specific files for different deployments:
#    - .env.development (local dev)
#    - .env.production (production settings)
#    - .env.test (testing environment)
# 
# 4. For GitHub Actions, set secrets in:
#    Repository Settings > Secrets and variables > Actions
# 
# 5. For Docker deployment, pass environment variables via:
#    - docker run -e VAR=value
#    - docker-compose.yml environment section
#    - Kubernetes ConfigMap/Secret
# 
# 6. Frontend (Vite) variables MUST start with VITE_ prefix
#    to be exposed to the browser
# 
# ============================================================================
